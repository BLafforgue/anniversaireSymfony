{% extends 'base.html.twig' %}

{% block title %}Member{% endblock %}

{% block body %}
    <h1>{{ member.lastname }} {{ member.firstname }}</h1>

    <b>Date : </b> {{ member.birthday ? member.birthday|date('Y-m-d') : '' }}

    <div id="card">
        <b>Nom de famille : </b> {{ member.lastname }} <br>
        <b>Prénom : </b> {{ member.firstname }} <br>
        <b>Date : </b> {{ member.birthday ? member.birthday|date('Y-m-d') : '' }} <br>
        <a  class="button-4" href="{{ path('app_member_edit', {'id': member.id}) }}"><i class="fa-solid fa-pen-to-square"></i> Modifier</a>
        <a  class="button-4" href="{{ path('app_member_delete', {'id': member.id}) }}"><i class="fa-solid fa-trash-can"></i> Supprimer</a>
    </div>

    <a href="{{ path('app_member_index') }}">back to list</a>

    <a href="{{ path('app_member_edit', {'id': member.id}) }}">edit</a>

    {{ include('member/_delete_form.html.twig') }}
{% endblock %}
